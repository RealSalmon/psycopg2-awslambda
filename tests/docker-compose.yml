version: '3'
services:

  postgresql:
    image: "postgres:9.6"
    logging:
      driver: none

  python:
    build:
      context: .
      dockerfile: Dockerfile.tests.python
    volumes:
     - .:/tests
    command: pytest /tests
    depends_on:
      - postgresql
    environment:
      - PGHOST=postgresql
